const countTens = (numbers) => {
  let tens = 0;
  for (let i = 0; i < numbers.length; i++) {
    if (numbers[i] === 10) {
      tens++
    }
  }
  return tens
}

const sum =(numbers) => {
  let sums = 0
  for (let i = 0; i < numbers.length; i++){
     result = sums + numbers[i]
  }
}